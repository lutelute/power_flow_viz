# dc_accuracy_analysis.html - DC潮流精度検証・分析ツール

## 📋 概要

`dc_accuracy_analysis.html`は、DC潮流計算の精度特性を定量的に分析し、AC潮流解との比較検証を行う専門的分析ツールです。DC近似の妥当性評価、適用限界の把握、無効電力影響の定量化などを通じて、DC潮流の実用性を科学的に評価できます。

## 🎯 主要機能

### DC潮流精度分析

#### DC近似の基本原理
```
AC潮流方程式:
P_i = Σ|V_i||V_j||Y_ij|cos(θ_ij - δ_i + δ_j)
Q_i = Σ|V_i||V_j||Y_ij|sin(θ_ij - δ_i + δ_j)

DC近似仮定:
1. |V_i| ≈ 1.0 (pu)         [電圧大きさ一定]
2. θ_ij ≈ 90° (r << x)      [リアクタンス主体]  
3. δ_i - δ_j ≪ 1           [小位相差]
4. cos(δ_i - δ_j) ≈ 1      [位相差近似]
5. sin(δ_i - δ_j) ≈ δ_i - δ_j [線形近似]

DC潮流方程式:
P = B × δ                   [線形関係]
```

#### 精度評価指標

**1. 位相角誤差**
```
δ_error = |δ_DC - δ_AC|     [rad]
δ_error% = δ_error/δ_AC × 100  [%]
```

**2. 電力潮流誤差** 
```
P_error = |P_DC - P_AC|     [MW]
P_error% = P_error/P_AC × 100  [%]
```

**3. 電圧誤差（間接評価）**
```
ΔV ≈ ΔQ/B_ii              [無効電力影響]
V_deviation = |1.0 - |V_AC|| [pu]
```

### 系統条件別精度特性

#### IEEE標準系統での精度評価

**IEEE 5母線系統**
- **系統特性**: 単純放射状、軽負荷
- **典型精度**: 位相角誤差 < 0.1°, 電力誤差 < 2%
- **適用性**: 良好、経済負荷配分に適用可能

**IEEE 9母線系統（WSCC）**
- **系統特性**: ループ系統、中程度負荷
- **典型精度**: 位相角誤差 < 0.5°, 電力誤差 < 5%
- **適用性**: 概算計算、系統計画に適用可能

**IEEE 14母線系統**
- **系統特性**: 実用規模、変圧器含有
- **典型精度**: 位相角誤差 < 1.0°, 電力誤差 < 8%
- **適用性**: 短絡容量、供給予備力計算に適用

**IEEE 30母線系統**
- **系統特性**: 大規模、複雑接続
- **典型精度**: 位相角誤差 < 2.0°, 電力誤差 < 12%
- **適用性**: 傾向分析、感度解析に限定

## 🖥️ ユーザーインターフェース

### 分析画面構成

```
┌─────────────────────────────────────────────────────────────┐
│ DC潮流精度検証システム                                       │
├─────────────────────────────────────────────────────────────┤
│ 解析設定: [IEEE 14母線 ▼] [負荷係数: 1.0] [実行] [比較]     │
│ 表示: ☑位相角 ☑電力潮流 ☑統計 ☑グラフ [エクスポート]       │
├───────────────┬─────────────────┬─────────────────────────────┤
│   精度分析    │   系統図        │        統計情報             │
│              │                │                             │
│位相角誤差(°) │    ○─1─○      │ ┌─ 精度統計 ──────────────┐ │
│ 2.0 │  ◆     │   /     \      │ │平均位相角誤差: 0.73°      │ │
│     │   ◆    │  2       3     │ │最大位相角誤差: 1.45°      │ │  
│ 1.0 │    ◆   │  │       │     │ │平均電力誤差: 4.2%        │ │
│     │     ◆  │  ○───○───○    │ │最大電力誤差: 8.7%        │ │
│ 0.0 └───────── │  4   5   6     │ │DC近似妥当性: 良好        │ │
│     2  4  6  8│                │ └─────────────────────────┘ │
│     母線番号  │                │                             │
├───────────────┼─────────────────┼─────────────────────────────┤
│ 誤差マップ    │ 無効電力影響    │      改善提案               │
│              │                │                             │
│   高精度      │ Q vs ΔV関係    │ 1. 送電線R/X比改善で精度向上 │
│ ■■■■□□  │                │ 2. 重負荷時はAC潮流推奨     │
│ ■■■□□□  │ ΔV │   ●       │ 3. 無効電力制約考慮が必要   │  
│ ■■□□□□  │    │  ●         │ 4. 電圧制約は別途確認要     │
│   低精度      │    │ ●          │                             │
│              │    └────► ΔQ   │                             │
└───────────────┴─────────────────┴─────────────────────────────┘
```

### 詳細分析タブ

#### 母線別精度分析
```
母線別DC近似精度 (IEEE 14母線系統)
═══════════════════════════════════════════════════
母線  種別    AC解析        DC解析       誤差分析
No.  Type   δ(°)  |V|(pu)  δ(°) |V|   Δδ(°) Δ|V|
---  ---- -------- ------- ----- ---- ------ ----
 1   Slack   0.00   1.060   0.00  1.0   0.00  0.06
 2   PV     -4.98   1.045  -4.73  1.0   0.25  0.05
 3   PQ    -12.72   1.010 -12.14  1.0   0.58  0.01
 4   PQ    -10.31   1.019 -9.89   1.0   0.42  0.02
 5   PQ     -8.78   1.020 -8.45   1.0   0.33  0.02
 6   PQ    -14.22   1.070 -13.71  1.0   0.51  0.07
 7   PQ    -13.36   1.062 -12.91  1.0   0.45  0.06
 8   PQ    -13.36   1.090 -12.91  1.0   0.45  0.09
 9   PQ    -14.94   1.056 -14.38  1.0   0.56  0.06
10   PQ    -15.10   1.051 -14.52  1.0   0.58  0.05
11   PQ    -14.79   1.057 -14.23  1.0   0.56  0.06
12   PQ    -15.07   1.055 -14.50  1.0   0.57  0.06
13   PQ    -15.16   1.050 -14.58  1.0   0.58  0.05
14   PQ    -16.03   1.036 -15.41  1.0   0.62  0.04
---  ---- -------- ------- ----- ---- ------ ----
統計       Mean:                    0.47  0.04
          Max:                     0.62  0.09
          RMS:                     0.51  0.05
```

#### 送電線別精度分析
```
送電線別電力潮流精度 (IEEE 14母線系統)
════════════════════════════════════════════════
送電線   AC解析      DC解析      誤差分析
From-To  P(MW) Q(MVAr) P(MW) Q  ΔP(MW) ΔP(%)
-------  ----- ------ ----- -- ------ -----
 1- 2    156.9   75.5  152.3  0   4.6   2.9
 1- 5     75.5   18.6   78.1  0  -2.6  -3.4
 2- 3     73.2   16.9   75.8  0  -2.6  -3.5
 2- 4     56.1    5.0   58.3  0  -2.2  -3.9
 2- 5     41.5   17.6   39.9  0   1.6   3.8
 3- 4    -23.3   -3.7  -21.8  0  -1.5   6.4
 4- 5    -61.2   15.8  -59.7  0  -1.5   2.4
 4- 7     28.1   -9.4   29.2  0  -1.1  -3.9
 4- 9     16.1   -1.5   17.3  0  -1.2  -7.5
 5- 6     44.1   12.2   42.7  0   1.4   3.2
 6-11     7.4    3.6    8.1   0  -0.7  -9.5
 6-12     7.8    2.5    8.4   0  -0.6  -7.7
 6-13     17.8    7.2   18.9  0  -1.1  -6.2
 7- 8      0.0    -17.4  0.0  0   0.0    -
 7- 9     28.1    8.1   29.2  0  -1.1  -3.9
 9-10      5.2    1.9    6.1  0  -0.9 -17.3
 9-14      9.4    3.6   10.5  0  -1.1 -11.7
10-11     -3.8   -1.6   -3.1  0  -0.7  18.4
12-13      1.6    0.8    2.1  0  -0.5 -31.3
13-14      5.6    1.8    6.2  0  -0.6 -10.7
-------  ----- ------ ----- -- ------ -----
統計      Mean Error:              1.4   8.2
          Max Error:               4.6  31.3
          RMS Error:               1.8  11.6
```

## 🔬 技術実装

### 精度分析エンジン

#### DCAccuracyAnalyzer クラス
```javascript
class DCAccuracyAnalyzer {
    constructor() {
        this.acSolution = null;
        this.dcSolution = null;
        this.accuracyMetrics = {};
    }
    
    // DC vs AC 精度比較分析
    performAccuracyAnalysis(systemData) {
        // AC潮流計算（基準解）
        this.acSolution = this.runACPowerFlow(systemData);
        
        // DC潮流計算（比較対象）
        this.dcSolution = this.runDCPowerFlow(systemData);
        
        // 精度指標計算
        this.accuracyMetrics = this.calculateAccuracyMetrics();
        
        // 統計分析
        const statistics = this.performStatisticalAnalysis();
        
        // 妥当性評価
        const validity = this.assessDCValidation();
        
        return {
            acSolution: this.acSolution,
            dcSolution: this.dcSolution,
            metrics: this.accuracyMetrics,
            statistics: statistics,
            validity: validity,
            recommendations: this.generateRecommendations()
        };
    }
    
    // DC潮流計算実装
    runDCPowerFlow(systemData) {
        // アドミタンス行列からサセプタンス行列構築
        const B = this.buildBMatrix(systemData.branch);
        
        // 指定有効電力ベクトル
        const P_spec = this.buildPSpecVector(systemData.bus, systemData.gen);
        
        // DC潮流方程式: P = B × δ
        // δ = B^(-1) × P （Slack母線除く）
        const B_reduced = this.removeSlackBus(B);
        const P_reduced = this.removeSlackBus(P_spec);
        
        const delta = this.solveLinearSystem(B_reduced, P_reduced);
        
        // 送電線電力潮流計算
        const branchFlows = this.calculateDCBranchFlows(delta, systemData.branch);
        
        return {
            bus: this.formatBusResults(delta),
            branch: branchFlows,
            converged: true,
            method: 'DC Power Flow'
        };
    }
    
    // サセプタンス行列構築（DC用）
    buildBMatrix(branchData) {
        const nbus = this.getMaxBusNumber(branchData);
        const B = new Array(nbus).fill(0).map(() => new Array(nbus).fill(0));
        
        branchData.forEach(branch => {
            const from = branch.fbus - 1;  // 0-indexed
            const to = branch.tbus - 1;
            
            // サセプタンス: b = x / (r² + x²) ≈ 1/x (r << x 仮定)
            const x = branch.x;
            const b = 1.0 / x;
            
            // B行列構築（対称行列）
            B[from][from] += b;  // 自己アドミタンス
            B[to][to] += b;
            B[from][to] -= b;    // 相互アドミタンス
            B[to][from] -= b;
        });
        
        return B;
    }
    
    // 精度指標計算
    calculateAccuracyMetrics() {
        const metrics = {
            bus: [],
            branch: [],
            overall: {}
        };
        
        // 母線別精度指標
        for (let i = 0; i < this.acSolution.bus.length; i++) {
            const ac_bus = this.acSolution.bus[i];
            const dc_bus = this.dcSolution.bus[i];
            
            // 位相角誤差
            const delta_error = Math.abs(dc_bus.va - ac_bus.va);
            const delta_error_percent = (delta_error / Math.abs(ac_bus.va)) * 100;
            
            // 電圧大きさ誤差（DC仮定値1.0 vs AC解）
            const voltage_error = Math.abs(1.0 - ac_bus.vm);
            
            metrics.bus.push({
                id: ac_bus.id,
                type: ac_bus.type,
                delta_error_deg: delta_error * 180 / Math.PI,
                delta_error_percent: delta_error_percent,
                voltage_error_pu: voltage_error,
                ac_angle: ac_bus.va * 180 / Math.PI,
                dc_angle: dc_bus.va * 180 / Math.PI,
                ac_voltage: ac_bus.vm,
                dc_voltage: 1.0
            });
        }
        
        // 送電線別精度指標
        for (let i = 0; i < this.acSolution.branch.length; i++) {
            const ac_branch = this.acSolution.branch[i];
            const dc_branch = this.dcSolution.branch[i];
            
            // 有効電力誤差
            const p_error = Math.abs(dc_branch.pf - ac_branch.pf);
            const p_error_percent = (p_error / Math.abs(ac_branch.pf)) * 100;
            
            // 無効電力誤差（DCでは0と仮定）
            const q_error = Math.abs(ac_branch.qf);  // AC解の無効電力そのもの
            
            metrics.branch.push({
                from: ac_branch.fbus,
                to: ac_branch.tbus,
                p_error_mw: p_error,
                p_error_percent: p_error_percent,
                q_neglect_mvar: q_error,
                ac_pf: ac_branch.pf,
                dc_pf: dc_branch.pf,
                ac_qf: ac_branch.qf
            });
        }
        
        return metrics;
    }
}
```

### 統計分析システム

#### StatisticalAnalyzer クラス
```javascript
class StatisticalAnalyzer {
    // 精度統計分析
    analyzeAccuracy(metrics) {
        const busMetrics = metrics.bus;
        const branchMetrics = metrics.branch;
        
        return {
            bus_accuracy: {
                angle: {
                    mean_error: this.mean(busMetrics.map(m => m.delta_error_deg)),
                    max_error: Math.max(...busMetrics.map(m => m.delta_error_deg)),
                    rms_error: this.rms(busMetrics.map(m => m.delta_error_deg)),
                    std_deviation: this.std(busMetrics.map(m => m.delta_error_deg))
                },
                voltage: {
                    mean_deviation: this.mean(busMetrics.map(m => m.voltage_error_pu)),
                    max_deviation: Math.max(...busMetrics.map(m => m.voltage_error_pu)),
                    rms_deviation: this.rms(busMetrics.map(m => m.voltage_error_pu))
                }
            },
            
            branch_accuracy: {
                power: {
                    mean_error_mw: this.mean(branchMetrics.map(m => m.p_error_mw)),
                    max_error_mw: Math.max(...branchMetrics.map(m => m.p_error_mw)),
                    mean_error_percent: this.mean(branchMetrics.map(m => m.p_error_percent)),
                    max_error_percent: Math.max(...branchMetrics.map(m => m.p_error_percent)),
                    rms_error_percent: this.rms(branchMetrics.map(m => m.p_error_percent))
                },
                reactive: {
                    total_neglected_mvar: branchMetrics.reduce((sum, m) => 
                                           sum + Math.abs(m.q_neglect_mvar), 0),
                    max_neglected_mvar: Math.max(...branchMetrics.map(m => 
                                                 Math.abs(m.q_neglect_mvar)))
                }
            }
        };
    }
    
    // DC近似妥当性評価
    assessDCValidation(statistics, systemData) {
        const criteria = {
            excellent: { angle: 0.5, power: 3.0 },
            good: { angle: 1.0, power: 5.0 },
            acceptable: { angle: 2.0, power: 10.0 },
            poor: { angle: 5.0, power: 20.0 }
        };
        
        const maxAngleError = statistics.bus_accuracy.angle.max_error;
        const maxPowerError = statistics.branch_accuracy.power.max_error_percent;
        
        let accuracy_level = 'poor';
        if (maxAngleError <= criteria.excellent.angle && 
            maxPowerError <= criteria.excellent.power) {
            accuracy_level = 'excellent';
        } else if (maxAngleError <= criteria.good.angle && 
                   maxPowerError <= criteria.good.power) {
            accuracy_level = 'good';
        } else if (maxAngleError <= criteria.acceptable.angle && 
                   maxPowerError <= criteria.acceptable.power) {
            accuracy_level = 'acceptable';
        }
        
        return {
            level: accuracy_level,
            angle_compliance: maxAngleError <= criteria.acceptable.angle,
            power_compliance: maxPowerError <= criteria.acceptable.power,
            recommended_applications: this.getRecommendedApplications(accuracy_level),
            limitations: this.getKnownLimitations(accuracy_level),
            improvement_suggestions: this.getImprovementSuggestions(statistics, systemData)
        };
    }
}
```

## 📊 分析結果例

### IEEE 14母線系統での典型的分析結果

#### 全体精度サマリー
```
DC潮流精度分析結果 (IEEE 14母線系統)
══════════════════════════════════════

【位相角精度】
- 平均誤差: 0.47°
- 最大誤差: 0.62° (母線14)
- RMS誤差: 0.51°
- 標準偏差: 0.13°

【電力潮流精度】  
- 平均誤差: 1.4 MW (8.2%)
- 最大誤差: 4.6 MW (31.3%)
- RMS誤差: 1.8 MW (11.6%)

【電圧偏差】
- 平均偏差: 0.04 pu
- 最大偏差: 0.09 pu (母線8)
- DC仮定妥当性: 概ね良好

【総合評価】
精度レベル: 良好 (Good)
推奨用途: 系統計画、感度解析、概算計算
制約: 詳細解析には不適、無効電力制約は別途考慮要
```

#### 負荷条件別精度特性

| 負荷係数 | 平均角度誤差 | 最大角度誤差 | 平均電力誤差 | 精度評価 |
|----------|--------------|--------------|--------------|----------|
| 0.5 (軽負荷) | 0.23° | 0.31° | 3.2% | 優秀 |
| 1.0 (定格負荷) | 0.47° | 0.62% | 8.2% | 良好 |
| 1.3 (重負荷) | 0.71° | 1.02° | 14.5% | 可 |
| 1.5 (過負荷) | 1.15° | 1.78° | 22.3% | 不可 |

#### 系統特性別適用性評価

```
DC潮流適用性マトリクス
═══════════════════════════════════════

系統特性          精度  速度  メモリ 総合評価 推奨度
-------------------------------------------------
軽負荷放射状       A    A     A      A       ★★★
中負荷ループ       B    A     A      B       ★★☆  
重負荷メッシュ     C    A     A      C       ★☆☆
高R/X送電線       D    A     A      D       ☆☆☆
変圧器多数含有     C    A     A      C       ★☆☆
長距離送電主体     B    A     A      B       ★★☆

A: 優秀, B: 良好, C: 可, D: 不可
★★★: 強く推奨, ★★☆: 推奨, ★☆☆: 条件付推奨, ☆☆☆: 非推奨
```

## 🎓 教育価値

### 近似手法の理解促進

#### 物理的近似の理解
1. **線形化概念**: 非線形問題の線形近似の意味
2. **工学的判断**: 精度とコストのトレードオフ
3. **適用限界**: 近似が破綻する条件の理解
4. **検証の重要性**: 近似解の妥当性確認方法

#### 数値解析スキル
1. **誤差評価**: 数値誤差の定量的評価手法
2. **統計分析**: 多数のデータからの知見抽出
3. **検証手法**: 解の正しさを確認する方法
4. **改善方針**: 精度向上のための方策検討

### 実用的判断力

#### 工学的センス
1. **用途適性**: 問題に応じた手法選択
2. **精度要求**: 必要精度の合理的設定
3. **効率性**: 計算資源の効果的利用
4. **リスク管理**: 近似誤差のリスク評価

#### 実務応用力
1. **系統解析**: 実系統での近似手法活用
2. **計画立案**: 長期計画での概算手法利用
3. **運用判断**: 運用時の高速計算ニーズ
4. **品質管理**: 計算結果の信頼性確保

## 🔧 カスタマイズ

### 分析条件設定

#### 負荷条件変更
```javascript
const loadScenarios = {
    light_load: {
        factor: 0.5,
        description: '軽負荷条件（夜間・休日）',
        typical_accuracy: 'excellent'
    },
    
    normal_load: {
        factor: 1.0, 
        description: '定格負荷条件（平日昼間）',
        typical_accuracy: 'good'
    },
    
    heavy_load: {
        factor: 1.3,
        description: '重負荷条件（夏期ピーク）',
        typical_accuracy: 'acceptable'
    },
    
    contingency: {
        factor: 1.2,
        modification: 'remove_critical_line',
        description: 'N-1事故時条件',
        typical_accuracy: 'poor'
    }
};
```

#### 精度基準カスタマイズ
```javascript
const accuracyCriteria = {
    transmission_planning: {
        angle_tolerance: 1.0,    // 度
        power_tolerance: 5.0,    // %
        application: '送電計画用'
    },
    
    economic_dispatch: {
        angle_tolerance: 0.5,
        power_tolerance: 3.0,
        application: '経済負荷配分用'
    },
    
    contingency_analysis: {
        angle_tolerance: 2.0,
        power_tolerance: 10.0,
        application: 'N-1解析用'
    },
    
    real_time_operation: {
        angle_tolerance: 0.2,
        power_tolerance: 2.0,
        application: 'リアルタイム運用'
    }
};
```

## 🚀 応用・発展

### 研究応用

#### 近似理論研究
- **高次近似**: DC以上の精度を持つ高速手法
- **適応的近似**: 系統状態に応じた近似切替
- **確率的解析**: 不確実性を考慮した近似精度
- **機械学習**: AI による近似精度予測

#### 実用化研究
- **大規模系統**: 超大規模系統でのスケーラビリティ
- **実時間制約**: ミリ秒オーダーの計算時間要求
- **組込みシステム**: 限定計算資源での実装
- **分散計算**: 並列・分散処理による高速化

### 産業応用

#### 系統運用支援
- **瞬時計算**: 運用者の意思決定支援
- **感度分析**: パラメータ変更影響の瞬時評価
- **最適化前処理**: 詳細最適化の初期解生成
- **アラーム処理**: 異常検出の高速スクリーニング

#### 計画業務効率化
- **概算検討**: 初期検討段階での高速評価
- **ケーススタディ**: 多数シナリオの一括処理
- **感度分析**: 設計パラメータの影響度評価
- **経済評価**: 投資効果の概算評価

---

**ファイル**: `dc_accuracy_analysis.html`  
**作成日**: 2024年12月30日  
**更新日**: 2024年12月30日  
**バージョン**: 1.3